# MongoDB Atlas connection string
MONGODB_URI="mongodb+srv://usuario:password@cluster.xxxx.mongodb.net/comentarios?retryWrites=true&w=majority"
# Secreto para firmar sesiones (genera uno: openssl rand -base64 32)
JWT_SECRET="cambia-esto-por-un-secreto-largo-y-aleatorio"
# Token de Vercel Blob (Storage -> Blob -> conectar). Solo necesario para subir evidencias.
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxxxxxxx"
